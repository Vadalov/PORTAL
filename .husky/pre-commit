# Run lint-staged for code quality checks
npx lint-staged

# Note: Typecheck and tests are disabled in pre-commit hook
# due to existing errors. They run in CI/CD instead.
# Once errors are fixed, uncomment these:
# npm run typecheck
# npm run test:run
