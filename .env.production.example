# =============================================================================
# PORTAL - Production Environment Configuration Template
# =============================================================================
#
# Instructions:
# 1. Copy this file to .env.production (DO NOT commit .env.production)
# 2. Fill in all REQUIRED variables
# 3. Configure OPTIONAL variables based on your needs
# 4. Set these same variables in Vercel/Railway dashboard
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Backend Configuration
# -----------------------------------------------------------------------------

# Convex Backend URL (REQUIRED)
# Get this from: npx convex deploy --prod
# Example: https://your-project-name.convex.cloud
NEXT_PUBLIC_CONVEX_URL=https://your-production-deployment.convex.cloud

# Backend Provider (REQUIRED - DO NOT CHANGE)
BACKEND_PROVIDER=convex
NEXT_PUBLIC_BACKEND_PROVIDER=convex

# -----------------------------------------------------------------------------
# REQUIRED - Security Secrets
# -----------------------------------------------------------------------------

# CSRF Protection Secret (REQUIRED)
# Generate with: openssl rand -base64 32
# Must be at least 32 characters
CSRF_SECRET=your-production-csrf-secret-min-32-chars

# Session Secret (REQUIRED)
# Generate with: openssl rand -base64 32
# Must be at least 32 characters
SESSION_SECRET=your-production-session-secret-min-32-chars

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Node Environment (REQUIRED)
NODE_ENV=production
NEXT_PUBLIC_NODE_ENV=production

# Application Metadata (OPTIONAL)
NEXT_PUBLIC_APP_NAME=Dernek Yönetim Sistemi
NEXT_PUBLIC_APP_VERSION=1.0.0

# Feature Flags (OPTIONAL)
NEXT_PUBLIC_ENABLE_REALTIME=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_KUMBARA=true

# -----------------------------------------------------------------------------
# Email Configuration (OPTIONAL - for notifications)
# -----------------------------------------------------------------------------

# SMTP Settings
# Required if you want to send email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@your-domain.com
SMTP_SECURE=false

# Email Templates
EMAIL_TEMPLATES_PATH=./email-templates

# -----------------------------------------------------------------------------
# SMS Configuration (OPTIONAL - Twilio)
# -----------------------------------------------------------------------------

# Twilio Configuration
# Required if you want to send SMS notifications
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+905xxxxxxxxx

# -----------------------------------------------------------------------------
# Google Maps (OPTIONAL - for Kumbara locations)
# -----------------------------------------------------------------------------

# Google Maps API Key
# Required if using Kumbara (donation box) location features
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# -----------------------------------------------------------------------------
# Monitoring & Analytics (OPTIONAL but RECOMMENDED)
# -----------------------------------------------------------------------------

# Sentry Error Tracking (RECOMMENDED)
# Sign up at: https://sentry.io
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ORG=your-org-name
SENTRY_PROJECT=your-project-name
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Google Analytics 4 (OPTIONAL)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Vercel Analytics (OPTIONAL - automatically enabled on Vercel)
# No configuration needed - enabled by default in production

# -----------------------------------------------------------------------------
# Database Backup (OPTIONAL)
# -----------------------------------------------------------------------------

# Convex Backup Configuration
# Convex automatically backs up your data
# Configure backup schedules in Convex dashboard

# -----------------------------------------------------------------------------
# Rate Limiting (OPTIONAL - defaults provided)
# -----------------------------------------------------------------------------

# Rate limit window in milliseconds (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# -----------------------------------------------------------------------------
# CORS Configuration (OPTIONAL)
# -----------------------------------------------------------------------------

# Allowed origins for CORS (comma-separated)
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com

# -----------------------------------------------------------------------------
# Deployment Configuration (Vercel)
# -----------------------------------------------------------------------------

# Vercel Configuration
# These are automatically set by Vercel - DO NOT set manually
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-deployment.vercel.app
# VERCEL_GIT_COMMIT_SHA=abc123...

# Recommended Vercel Region: Frankfurt (fra1) for Turkey
# Set in vercel.json or Vercel dashboard

# -----------------------------------------------------------------------------
# Performance & Optimization
# -----------------------------------------------------------------------------

# Next.js Telemetry (disable in production for privacy)
NEXT_TELEMETRY_DISABLED=1

# Build Optimization
ANALYZE=false

# Image Optimization
NEXT_PUBLIC_IMAGE_DOMAINS=your-cdn-domain.com

# -----------------------------------------------------------------------------
# Security Headers (configured in next.config.ts)
# -----------------------------------------------------------------------------

# Security headers are automatically applied:
# - HSTS (Strict-Transport-Security)
# - X-Frame-Options: DENY
# - X-Content-Type-Options: nosniff
# - Referrer-Policy: origin-when-cross-origin
# - Content-Security-Policy (CSP)

# -----------------------------------------------------------------------------
# Deployment Checklist
# -----------------------------------------------------------------------------

# Before deploying to production:
# [ ] Set all REQUIRED environment variables
# [ ] Generate secure CSRF_SECRET and SESSION_SECRET (min 32 chars)
# [ ] Deploy Convex backend: npx convex deploy --prod
# [ ] Set NEXT_PUBLIC_CONVEX_URL from Convex deployment
# [ ] Configure SMTP/Twilio if using notifications
# [ ] Set up Sentry for error monitoring
# [ ] Enable Vercel Analytics
# [ ] Test health endpoint: https://your-domain/api/health?detailed=true
# [ ] Configure custom domain in Vercel
# [ ] Set up SSL certificate (automatic on Vercel)
# [ ] Run security audit: npm audit
# [ ] Test authentication flow
# [ ] Verify CSRF protection is working
# [ ] Test rate limiting
# [ ] Review and rotate secrets regularly (every 90 days)

# -----------------------------------------------------------------------------
# Support & Documentation
# -----------------------------------------------------------------------------

# For detailed deployment instructions, see:
# - docs/DEPLOYMENT_QUICKSTART.md
# - README.md
# - .github/copilot-instructions.md

# For security best practices, see:
# - docs/KVKK_GDPR_COMPLIANCE.md

# =============================================================================
# END OF CONFIGURATION
# =============================================================================
